<% if notice.present? %>
  <%= content_tag(:p, notice, class: 'inline-block px-3 py-2 mb-5 font-medium bg-black text-white', id: 'notice') %>
<% end %>

<section class="absolute inset-0 grid w-screen h-screen text-4xl font-bold place-items-center">
  <%= image_tag('logo.png', class: 'animate-pulse h-14', alt: 'Логотип INKZ') %>
</section>