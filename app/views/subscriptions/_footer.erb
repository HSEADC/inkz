<%= form_with(model: subscription) do |form| %>

  <% if subscription.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(subscription.errors.count, "error") %> prohibited this subscription from being saved:</h2>
      <ul>

        <% subscription.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>

      </ul>
    </div>
  <% end %>

  <div class="flex gap-4">
    <%= form.email_field :email, class: 'px-5 py-3 text-xl bg-transparent border-2 border-white rounded-xl placeholder:text-white', placeholder: 'Введите почту' %>
    <%= form.submit 'Подписаться', class: 'px-10 py-3 ml-4 text-2xl text-black duration-200 bg-white hover:bg-white/85 rounded-xl' %>
  </div>

<% end %>
