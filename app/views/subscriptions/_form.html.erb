<%= form_with(model: subscription) do |form| %>
  <% if subscription.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(subscription.errors.count, "error") %> prohibited this subscription from being saved:</h2>

      <ul>
        <% subscription.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="flex justify-center gap-2">
    <%= form.email_field :email, class: 'text-2xl rounded-full bg-transparent border border-black placeholder:text-black', placeholder: 'Введите почту' %>
    <%= form.submit 'Отправить', class: 'text-2xl rounded-full bg-black text-white px-6 py-3' %>
  </div>
<% end %>
